# ピルログ開発 - 次回スタートアップガイド

> **作成日**: 2026-07-05（Day 14）
> **次回セッション**: 無料化ビルドの実機確認 → App Store 申請フェーズへ

---

## 📋 前回（Day 14）の到達点

**全機能無料化を決定・実装した。**

- ✅ Premium機能の訴求力・開発者名のブランドリスクについて協議 → 「全機能無料化」「開発者名分離は保留」で決定
- ✅ `lib/premium.ts` に `FREE_LAUNCH = true` フラグを追加、RevenueCat設定・購入フローは温存
- ✅ 複数ピル登録は「ゲート解除のみ」で確定（実機能構築はスコープ外）
- ✅ App Store説明文・利用規約・プライバシーポリシー・ロードマップを無料化に合わせて更新
- ✅ プライバシーポリシーにApple ヘルスケア（睡眠・心拍）のデータ収集を新規に明記

---

## 🎯 次回セッションの優先タスク

### 🔴 優先度★★★ - 無料化ビルドの作成・実機確認

コードは無料化済みだが、**Build 22 は無料化前のビルド**。`FREE_LAUNCH = true` を含む新しいビルドを作成し、実機で最終確認する。

#### 1. ビルド作成

```bash
npx eas build --platform ios --profile production
```

- QAビルド（`EXPO_PUBLIC_QA_PREMIUM=1`）は不要になった（`FREE_LAUNCH` が既に全ユーザーをPremium扱いにするため）。QAプロファイルの扱いは要検討（残しても害はないが、素直に本番プロファイルで検証してよい）。

#### 2. 実機確認チェックリスト

| # | 確認内容 | 期待結果 |
|---|---------|---------|
| 1 | 履歴画面で7日以上前にスクロール | ペイウォールが出ず閲覧できる |
| 2 | 履歴画面で7日以上前の日記を編集 | ロックされず編集できる |
| 3 | 設定 → Appleヘルスケア | Premiumバッジが出ず、そのまま接続・相関グラフ表示できる |
| 4 | 設定 → 「新しいピルを追加」 | ペイウォールに遷移せず「準備中」アラートが出る |
| 5 | 履歴画面下部 | 「Premiumでできること」カードが表示されない |

### 🔴 優先度★★★ - App Store 申請準備

#### 1. スクリーンショット撮り直し（必須）

Phase 1 時点・Premium訴求ありの古いスクリーンショットが残っている可能性がある。無料化後の実機で撮り直す。

**必要なシーン（6.7インチ）**:
| # | 画面 | ポイント |
|---|------|---------|
| 1 | ホーム（服薬リング + DoseCard） | コアUI |
| 2 | 履歴 + 1行日記 | 継続フック |
| 3 | 相関グラフ（服薬日数 × 睡眠 × 心拍） | 無料で使える目玉機能 |
| 4 | 設定（ヘルスケア連携済み状態、Premiumバッジなし） | 全機能無料であることが伝わる状態 |
| 5 | オンボーディング or ホーム別カット | 任意 |

> ⚠️ ペイウォール画面・Premiumバッジが写り込んだカットは使わない。

#### 2. App Store 説明文（更新済み・要確認のみ）

`docs/app-store/description.md` は本セッションでPremium/980円の記載を削除済み。App Store Connectへ転記する際、内容に齟齬がないか最終確認する。

#### 3. 審査ノート（App Review Notes）の準備

課金フローがなくなったため、**Sandboxテスターアカウントの提供は不要**になった。準備するのは:
- **HealthKitに関する説明**: 「睡眠と安静時心拍のみ読み取り」「書き込みはしない」「サーバーに送信しない」「完全ローカル処理」
- 「医療アドバイスではない」の免責表示がアプリ内にあることを明示

#### 4. App Store Connect 側の後片付け

- 課金製品 `jp.tasuku.pilllog.premium`（Non-Consumable）が App Store Connect に登録済みのはず。無料アプリとして申請する場合、この製品を非表示/削除するか、放置して問題ないか確認する（放置してもUI上は誰も到達しないが、審査担当が混乱する可能性があるため要確認）。
- 新バージョン（1.0.0）に無料化ビルドを選択 → スクリーンショット・説明文を更新 → 審査情報を記入 → 「審査へ提出」

---

### 🟡 優先度★★ - 残りの HealthKit 実機検証（H4-H7）

Day 13 から持ち越し。申請には必須ではないが、審査で落とされるリスクを減らせる。

| # | 操作 | 期待結果 |
|---|------|---------|
| H4 | HealthKit にデータがない期間 | 棒グラフのみ・折れ線が欠損しても崩れない |
| H5 | 権限を後から拒否 → 設定タブのヘルスケア行 | `Linking.openSettings()` で iOS 設定へ遷移 |
| H6 | グラフ画面 | 「医療アドバイスではありません」の免責表示あり |
| H7 | 旧バージョン（schema v2）からのアップデート | クラッシュなし、schema v3 `health_samples` テーブル正常生成 |

---

### 🟢 優先度★ - 次フェーズ（Phase 3・審査待ち期間の候補）

- **PostHog 統合**: イベント名のみ送信（健康データの中身は含めない）
- **気分（mood）× 服薬日数グラフ**: `daily_notes.mood` カラム準備済み
- **Claude API 月次レポート**: Vercel Edge Function 経由
- **Apple Developer アカウント分離**: 本業（エリアラン・アクセスプルナビ）と開発者名を分けて再リリースする方針。今回は据え置き、時間を置いてから着手（$99/年の追加アカウントで解決見込み）

---

## 💎 設計リファレンス（触る前に確認）

### Premium 判定（無料化により実質無効化・構造は温存）
- `usePremium()` → `{ isPremium, isLoading }` → `lib/premium.ts`
- **`FREE_LAUNCH = true`**（`lib/premium.ts:32`）— 常に Premium 扱い。再有料化する時はここを `false` に戻す
- entitlement: `'premium'` / QA バイパス: `EXPO_PUBLIC_QA_PREMIUM=1`（`FREE_LAUNCH` が優先されるため現状は無意味）
- `Purchases.configure()` は `app/_layout.tsx` の `initialize()` で 1 回のみ（変更なし）

### HealthKit データフロー（変更なし）
```
HealthDashboard
  → useQuery(['healthCorrelations', sheetId])
    → fetchCorrelationData()              [lib/health/queries.ts]
       → isCacheFresh()（24h TTL）
          stale → syncSleepSamples() / syncRestingHeartRate()  [lib/health/sync.ts]
       → buildWeeklyCorrelations()        [lib/health/correlations.ts]
          → WeekBucket[] → CorrelationChart（SVG）
```

---

## 📦 リファレンス情報

### App Store Connect
- アプリ名: ピルログ - シンプルなピル管理 / Bundle ID: `jp.tasuku.pilllog`
- Apple ID（ASC App ID）: **6772404120**
- [TestFlight](https://appstoreconnect.apple.com/apps/6772404120/testflight/ios)
- 課金製品: `jp.tasuku.pilllog.premium`（Non-Consumable、非消耗型）— **無料化に伴い要確認**（上記チェックリスト参照）

### EAS
- projectId: `23b7ca34-60ee-4ffa-a8fb-6c3ef942e6f9` / owner: `tasukuar`

### 最新ビルド
| # | 内容 | 状態 |
|---|------|------|
| Build 22 | @kingstinct v14 + Nitro Modules（HealthKit 新アーキ対応） | TestFlight 配信中（**無料化前のビルド**） |
| 次回ビルド | `FREE_LAUNCH = true` を含む | **要作成** |

---

## 🔧 申請前チェックリスト

- [ ] `FREE_LAUNCH = true` を含む新ビルドを作成・実機確認
- [ ] スクリーンショット5枚（6.7インチ）を撮り直し（ペイウォール/Premiumバッジなし）
- [ ] 審査ノート準備（HealthKit説明。Sandboxアカウントは不要）
- [ ] `jp.tasuku.pilllog.premium` の扱いを App Store Connect で確認
- [ ] `プライバシーポリシー.html` の HealthKit 記載を確認（本セッションで追加済み）
- [ ] App Store Connect で新ビルドを選択
- [ ] 「審査へ提出」

---

**作成日**: 2026-07-05
**作成者**: Claude Code
**次回目標**: 無料化ビルドの実機確認 → App Store 審査提出
